export function PanelMetric({label,value,trend,accent=""}:{label:string;value:string;trend?:string;accent?:string}){return <div className={`panel-metric ${accent}`}><span>{label}</span><strong>{value}</strong>{trend&&<small>{trend}</small>}</div>}
export function DemoNotice({isDemo}:{isDemo:boolean}){return isDemo?<div className="demo-notice"><span>ⓘ</span><p>Hay registros DEMO en esta vista. Son ficticios y no constituyen condiciones comerciales reales; las acciones quedan auditadas.</p></div>:null}
export function PanelTable({headers,rows}:{headers:string[];rows:string[][]}){return <div className="panel-table-wrap"><table className="panel-table"><thead><tr>{headers.map(h=><th key={h}>{h}</th>)}</tr></thead><tbody>{rows.map((row,i)=><tr key={i}>{row.map((cell,j)=><td key={j}>{j===row.length-1?<span className="table-status">{cell}</span>:cell}</td>)}</tr>)}</tbody></table></div>}
