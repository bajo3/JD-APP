"use client";
import { FormEvent,useState } from "react";
type Mode="contact"|"valuation"|"finder"; type ApiRecord=Record<string,unknown>; type FinderRow={vehicle:{slug:string;brand:string;model:string;year:number};statusLabel:string;reasonDetails?:string[]}; type FinderData={results?:FinderRow[]};
async function post(path:string,payload:ApiRecord):Promise<ApiRecord>{const r=await fetch(path,{method:"POST",headers:{"Content-Type":"application/json","Idempotency-Key":crypto.randomUUID()},body:JSON.stringify(payload)});const b=await r.json().catch(()=>({})) as ApiRecord;if(!r.ok)throw new Error(((b.error as ApiRecord|undefined)?.message as string|undefined)||"No pudimos procesar la consulta.");return b}
const money=(v:string)=>Number(v.replace(/\D/g,""))||0;
export function LeadForm({mode="contact"}:{mode?:Mode}){const [step,setStep]=useState(1),[busy,setBusy]=useState(false),[error,setError]=useState(""),[result,setResult]=useState<React.ReactNode>(null),[name,setName]=useState(""),[phone,setPhone]=useState(""),[consent,setConsent]=useState(mode==="finder");const submit=async(e:FormEvent<HTMLFormElement>)=>{e.preventDefault();setError("");const f=new FormData(e.currentTarget);if(!name.trim()||!phone.trim()||(mode!=="finder"&&!consent)){setError("Completá nombre, teléfono y consentimiento para continuar.");return}if(mode!=="contact"&&step===1){setStep(2);return}setBusy(true);try{if(mode==="valuation"){const l=await post("/api/v1/leads",{name,phone,contactConsent:true,source:"TASACION_WEB"});const a=await post("/api/v1/appraisals",{leadId:(l.data as ApiRecord).id,vehicle:{make:String(f.get("make")),model:String(f.get("model")),year:Number(f.get("year")),mileageKm:Number(f.get("km")),declaredCondition:String(f.get("condition"))}});const data=a.data as ApiRecord;setResult(<div className="form-success"><span>✓</span><h3>Solicitud recibida</h3><p>Código: <strong>{String(data.publicCode)}</strong><br/>Estado: {String(data.status)}</p></div>)}else if(mode==="finder"){const a=await post("/api/v1/affordability/search",{cash:money(String(f.get("cash"))),maxMonthlyPayment:money(String(f.get("monthly"))),acceptedTerms:[Number(f.get("term"))],preferences:{bodyTypes:[]}});setResult(<FinderResults data={a.data as FinderData}/>) }else{const l=await post("/api/v1/leads",{name,phone,contactConsent:true,source:"CONTACTO_WEB"});try{const h=await post("/api/v1/whatsapp/handoffs",{leadId:(l.data as ApiRecord).id,source:"CONTACTO_WEB"});const data=h.data as ApiRecord;setResult(<div className="form-success"><span>✓</span><h3>¡Gracias, {name}!</h3><p><a href={String(data.url)} target="_blank" rel="noreferrer">Abrir WhatsApp ↗</a></p></div>)}catch{setResult(<div className="form-success"><span>✓</span><h3>Consulta recibida</h3><p>WhatsApp no está configurado. Llamanos al <a href="tel:+5492494587046">249 458-7046</a>.</p></div>)}}}catch(err){setError(err instanceof Error?err.message:"Ocurrió un error. Intentá nuevamente.")}finally{setBusy(false)}};if(result)return result;return <form className="lead-form" onSubmit={submit}>{mode!=="contact"&&<div className="form-steps"><span className={step===1?"active":""}>01 Datos</span><span className={step===2?"active":""}>02 Preferencias</span></div>}<label>Nombre y apellido<input value={name} onChange={e=>setName(e.target.value)} placeholder="Ej. Martín González"/></label><label>Teléfono / WhatsApp<input value={phone} onChange={e=>setPhone(e.target.value)} placeholder="249 458-7046"/></label>{step===1&&mode!=="finder"&&<label className="consent-check"><input type="checkbox" checked={consent} onChange={e=>setConsent(e.target.checked)}/> Acepto que me contacten.</label>}{step===1&&mode==="contact"&&<label>Consulta<textarea name="message" rows={4}/></label>}{step===2&&mode==="valuation"&&<><label>Marca<input name="make" placeholder="Toyota"/></label><label>Modelo y versión<input name="model" placeholder="Corolla XEI"/></label><label>Año<input name="year" type="number" placeholder="2022"/></label><label>Kilómetros<input name="km" type="number" placeholder="48000"/></label><label>Estado<select name="condition" defaultValue="GOOD"><option value="EXCELLENT">Excelente</option><option value="GOOD">Bueno</option><option value="FAIR">Regular</option><option value="NEEDS_REPAIR">Necesita reparaciones</option></select></label></>}{step===2&&mode==="finder"&&<><label>Efectivo disponible<input name="cash" placeholder="$8.000.000"/></label><label>Cuota máxima<input name="monthly" placeholder="$600.000"/></label><label>Plazo<select name="term" defaultValue="48"><option value="36">36 meses</option><option value="48">48 meses</option><option value="60">60 meses</option></select></label></>}{mode==="contact"&&<label className="consent-check"><input type="checkbox" checked={consent} onChange={e=>setConsent(e.target.checked)}/> Acepto que me contacten.</label>}{error&&<p className="form-error" role="alert">{error}</p>}<button className="primary-button" disabled={busy}>{busy?"Enviando…":mode!=="contact"&&step===1?"Continuar":"Enviar consulta"}<span>→</span></button></form>}
function FinderResults({data}:{data:FinderData}){const rows=data.results??[];return <div className="finder-results"><h3>Opciones para tu presupuesto</h3>{rows.slice(0,4).map(x=><article key={x.vehicle.slug}><strong>{x.vehicle.brand} {x.vehicle.model}</strong><span>{x.vehicle.year} · {x.statusLabel}</span><small>{x.reasonDetails?.join(" · ")||"Evaluación orientativa."}</small></article>)}{!rows.length&&<p>No encontramos coincidencias. Probá ampliar cuota o plazo.</p>}<p className="finder-disclaimer">Resultado orientativo, sujeto a disponibilidad y aprobación financiera.</p></div>}
