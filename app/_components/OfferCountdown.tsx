"use client";
import { useEffect,useState } from "react";
export function OfferCountdown({endsAt}:{endsAt:string}){const [remaining,setRemaining]=useState(()=>Math.max(0,new Date(endsAt).getTime()-Date.now()));useEffect(()=>{const update=()=>setRemaining(Math.max(0,new Date(endsAt).getTime()-Date.now()));update();const timer=window.setInterval(update,1000);return()=>window.clearInterval(timer)},[endsAt]);if(remaining<=0)return <div className="countdown" role="status"><small>OFERTA</small><b>Oferta finalizada</b></div>;const total=Math.floor(remaining/1000),hours=Math.floor(total/3600),minutes=Math.floor(total%3600/60),seconds=total%60;return <div className="countdown" role="timer" aria-label={`La oferta termina en ${hours} horas, ${minutes} minutos y ${seconds} segundos`}><small>TERMINA EN</small><b aria-hidden="true">{String(hours).padStart(2,"0")} : {String(minutes).padStart(2,"0")} : {String(seconds).padStart(2,"0")}</b></div>}
